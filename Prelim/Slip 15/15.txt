// Question: Write a program in C#.Net to find the length of a string.

using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter a string: ");
        string input = Console.ReadLine();

        int length = input.Length;
        Console.WriteLine($"The length of the string is: {length}");
    }
}
